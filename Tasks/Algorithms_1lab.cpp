#include <bits/stdc++.h>
#include <conio.h>
using namespace std;
int main(){
 int Client_num,a;
 bool Found=false;
 cin>>Client_num;
 vector<int> Queue,lastQue(Client_num,0);

    for(int i=0;i<Client_num;i++){
        cin>>a;
        Queue.push_back(a);
    }
    for(int i=0;i<Client_num;i++){
        for
    }
    for(int i=0;i<Queue.size();i++){
        cout<<Queue[i]<<" ";
    }
    cin>>a;
}